'use strict';
describe('MyApp.job_view module', function ()
{
  beforeEach(module('myApp.job_view'));
  describe('job_view controller', function()
  {

    it ('should ...', inject(function($controller)
    {
      var jobViewCtrl= $controller('JobViewCtrl');
      expect(jobViewCtrl).toBeDefined();

    }));



  });

});
